{
  "name": "text-to-video-app",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "dependencies": { "express": "^4.18.2", "axios": "^1.4.0" }
}const express = require('express');
const axios = require('axios');
const app = express();

app.use(express.json());
app.use(express.static('public')); // Serves frontend files

// Mock response for testing (replace with real API later)
app.post('/generate-video', (req, res) => {
    const { prompt } = req.body;
    console.log('Received prompt:', prompt); // For debugging
    // Fake video URL (replace with real one)
    res.json({ videoUrl: 'https://sample-videos.com/zip/10/mp4/SampleVideo_1280x720_1mb.mp4' });
});

const listener = app.listen(process.env.PORT, () => {
    console.log('Your app is listening on port ' + listener.address().port);
});app.post('/generate-video', (req, res) => {
    // Mock response for testing
    res.json({ videoUrl: 'https://sample-videos.com/zip/10/mp4/SampleVideo_1280x720_1mb.mp4' }); // Replace with a real sample video URL
});app.post('/generate-video', async (req, res) => {
    const { prompt } = req.body;
    try {
        const response = await axios.post('https://api-inference.huggingface.co/models/stabilityai/stable-video-diffusion', {
            inputs: prompt, // Adjust based on model docs
            parameters: { num_frames: 16 } // Example params
        });
        // Hugging Face returns a blob; you might need to handle it differently (e.g., upload to a storage service)
        // For simplicity, assume it returns a URL (check docs)
        res.json({ videoUrl: response.data.video_url }); // Placeholder; adapt
    } catch (error) {
        res.status(500).json({ error: 'Failed: ' + error.message });
    }
});
